const enum Gender {
    Male = "male",
    Female = "female"
}

const enum 


class Employee {
    constructor(
        id: number,
        //personal info
        firstName: string, 
        lastName: string,
        gender: Gender,
        dob: string,
        age: number, 
        parent: boolean,
        maritalStatus: boolean,
        //job info
        position: string,

    )
}